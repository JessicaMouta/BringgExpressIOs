# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DMExpress' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'BringgTracking', :git => 'https://github.com/narlei/customer-sdk-ios.git', :branch => 'delivery'
  pod 'ARCarMovement'
  pod 'GoogleMaps'
  pod 'Kingfisher', '~> 4.10.1'
  # Pods for DMExpress

end
